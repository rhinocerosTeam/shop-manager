/**
 * Created by songpeilan on 2018/6/19.
 */
const config = {
  // 启动端口
  port: 3000,

  // 数据库配置
  database: {
    DATABASE: 'shop',
    USERNAME: 'root',
    PASSWORD: '@pwd=123456',
    PORT: '3306',
    HOST: 'songpeilan.cn'
  }
}

module.exports = config
